import { FilterMatchPipe,  } from './filter-match.pipe';

describe('FilterPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterMatchPipe();
    expect(pipe).toBeTruthy();
  });
});
