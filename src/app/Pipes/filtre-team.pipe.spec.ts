import { FiltreTeamPipe } from './filtre-team.pipe';

describe('FiltreTeamPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltreTeamPipe();
    expect(pipe).toBeTruthy();
  });
});
